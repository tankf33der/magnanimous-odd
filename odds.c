#include <stdio.h>
#include <math.h>
#include "gmp.h"

unsigned char a$;

int main(void) {
	// XXX fun starts here
	unsigned char a[4] = {1};
	unsigned char s[4];
	// ===
	
	a$ = sizeof(a);
	a[a$-1] = 0;
	

	// XXX (** 5 a$), max 27
	for(unsigned long i = 0; 625 > i; i++) {

	}
	s[0] = a[0] + 48;
	printf("'%c'\n", s[0]);
	
	
	return 0;
}
